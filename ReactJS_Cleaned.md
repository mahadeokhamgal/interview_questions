1. React Core Concepts
# What are keys in React and why are they important?
# What is reconciliation in React?
# How does React’s rendering process work? When does a component re-render?
# Explain the concept of lifting state up in React.
# What are React Fragments and why would you use them?
# How does React handle reconciliation? What is the role of keys in lists?
# What is prop drilling, and how can it be avoided?
# Explain the difference between Context API and prop drilling.
# How can you lift state up in React?
# What are some different ways to conditionally render elements in React?
# How to conditionally hide/show an element in React?
# How to achieve ngFor in React?
# How to achieve content projection in ReactJS?

2. React Hooks
# What are hooks in React? Name a few commonly used hooks.
# What are React hooks? Name a few common hooks other than useState and useEffect.
# What is the useEffect hook and how does it differ from useState?
# Explain how the useEffect hook works. What are some common pitfalls?
# What is the difference between useEffect and useLayoutEffect hooks?
# What is the difference between useLayoutEffect and useEffect in React?
# How does useReducer differ from useState in React?
# What is the difference between useMemo and useCallback hooks?
# What’s the difference between useMemo and useCallback? When should you use each?
# What is the difference between useRef and createRef?
# What is React.memo and when should you use it?
# What are custom hooks in React? Write a simple custom hook.
# Create a custom hook that calls a given function only once after the component mounts.
# How would you implement a debounced search input in React using hooks?

3. Forms & User Input
# How do you handle forms and user input in React?
# What is the difference between controlled and uncontrolled components in React?
# What are controlled vs uncontrolled forms in React?
# Explain the difference between controlled and uncontrolled components in React? When would you choose one?
# Build a controlled form component with validation.
# Why is it considered a bad practice to directly modify state in React? What is the proper way?

4. State Management
# What is Redux?
# Explain me Redux toolkit.
# How would you manage global state in a React application without Redux?
# What is React Context API and when would you use it?
# Explain React’s Context API and when to use it.

5. Advanced React Concepts
# What is higher-order component in ReactJS?
# What are Pure Components and how do they differ from regular components?
# What is React Portals and when would you use them?
# What are React Portals and when would you use them? (duplicate, keep once)
# How to create custom elements in ReactJS?
# Create a reusable modal component.

6. Performance Optimization
# How can you optimize performance in React applications?
# How would you optimize performance in a large React application?
# How do you implement code splitting in React?
# Error Handling & Testing
# How do you handle error boundaries in React?
# How do you test a React component?
# What is Storybook in ReactJS?

7. API & Authentication
# How does JWT work in ReactJS?
# What is React AuthGuard?
# What are React API interceptors?
# How would you make an API call when a component mounts?
# What is SSO (Single Sign-On)? (related to auth, but also used in React apps)

8. Ecosystem & Advanced Tools
# Describe how to implement lazy loading in React using React.lazy() and Suspense.
# What is Next.js used for?
# What is PWA (Progressive Web App)? (common with Angular/React)
# What is Storybook in ReactJS? (already in testing, mention once)